{
  "buildCommand": "cd focus-terminal && npm install && npm run build",
  "outputDirectory": "focus-terminal/dist/focus-terminal/browser",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}